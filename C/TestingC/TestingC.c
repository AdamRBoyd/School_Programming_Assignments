#include <stdio.h>

int main() {
    if (0xA)
        if (052)
            if ('\xeb')
                if ('\012')
                    printf("Tek Brains7");
                else;
            else;
        else;
    else;
}
